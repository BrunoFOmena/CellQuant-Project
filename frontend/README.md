# Frontend — Contador LCR (React + TypeScript)

```powershell
cd frontend
npm install
npm run dev
```

Abre em http://localhost:5173

O Vite encaminha `/exames` para o backend em http://localhost:8000.

Para o laboratório (um só processo Python), na raiz do projeto:

```powershell
.\cellquant start
```
